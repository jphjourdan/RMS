\begin{enonce}
\begin{exercise}[ID={RMS134 E931},subtitle={Mines-Ponts PSI
    2023},tags={}, concours=mines-ponts, filiere=PSI, annee=2023, theme=algebre, difficulty={0}]
  Soit $P\in\gl_n(\C)$ que l'on décompose en $P=Q + iR$ avec $Q, R\in\mat_n(\R)$.

  \begin{enumerate}
    \item 
      Montrer qu'il existe $\lambda\in\R$, tel que $Q + \lambda R\in\gl_n(\R)$.

    \item 
      En déduire que deux matrices $A$ et $B$ réelles, semblables sur $\C$, sont semblables sur $\R$.

    \item Soient $A,B\in\mat_n(\R)$ telles que $A^3 = B^3 = I_n$ et $\tr(A)=\tr(B)$.
      Montrer que $A$ et $B$ sont semblables.
  \end{enumerate}
\end{exercise}
\begin{solution}
\end{solution}
\end{enonce}
